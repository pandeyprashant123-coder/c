#include <stdio.h>
void calculate(int a,int b,int *p)
{
  *p=a+b;
}
int main() 
{
  // int a,*ptr;
  // a=10;
  // ptr=&a;
  // printf("Value of a=%d\taddress of a=%u\n",a,&a);
  // printf("Value of a=%d\taddress of a=%u\n",*ptr,ptr);

  //pointers in expression
  // int a=5,b=6,*c,*d,sum;
  // c=&a;
  // d=&b;
  // sum=*c+*d;
  // printf("%d",sum);


  //use of pointers
  // int a,b,sum;
  // calculate(4,5,&sum);
  // printf("%d",sum);

  int a[]={1,2,6,4,8},*ptr;
  int i;
  ptr=a;
  for (i=0;i<5;i++)
  {
    // printf("%d\n",*ptr+i);
    printf("%d\n",*ptr);
    ptr++;
  }
  return 0;
}
